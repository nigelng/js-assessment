if (typeof define !== 'function') { var define = require('amdefine')(module); }

define([ 'jquery' ], function($) {
  return {
    // Can use jQuery helpers for async
    async : function(value) {

    },

    manipulateRemoteData : function(url) {

    }
  };
});
